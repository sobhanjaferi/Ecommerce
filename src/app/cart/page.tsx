import OrderListContainer from '@/features/order/OrderListContainer'
import type { ReactElement } from 'react'

export default function CartPage(): ReactElement {
  return (
    <div>
      <OrderListContainer />
    </div>
  )
}
